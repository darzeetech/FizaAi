export type ProfileType = {
  tailor_name: string;
  tailor_count: number;

  profile_pic_url: Record<string, string>;
  boutique_details: Record<string, any>;
};

export type BoutiqueDetailsType = Record<string, any>;
